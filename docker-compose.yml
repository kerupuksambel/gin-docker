version: '2'
services:
  db:
    build: ./db
    container_name: crud-db
    command: 
      - --default-authentication-plugin=mysql_native_password
    volumes:
      - .:/application
    restart: always
    ports:
      - 10002:3306
  web:
    build: ./web
    container_name: crud-web
    environment:
      - PORT=3004
    ports:
      - 3004:3004
    depends_on:
      - db
